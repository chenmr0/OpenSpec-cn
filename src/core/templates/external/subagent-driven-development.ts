/**
 * Subagent-Driven Development External Skill Template
 *
 * Copied from superpowers-cn/skills/subagent-driven-development.
 * This skill is always installed during init alongside other external skills.
 * Spec reviewer and code quality reviewer are installed as agents (see agents/ directory).
 */
import type { SkillTemplate } from '../types.js';

const sddInstructions = `# 子智能体驱动开发

通过为每个任务分派一个全新的子智能体来执行计划，每个任务完成后进行两阶段审查：先审查规格合规性，再审查代码质量。

**为什么用子智能体：** 你将任务委派给具有隔离上下文的专用智能体。通过精心设计它们的指令和上下文，确保它们专注并成功完成任务。它们不应继承你的会话上下文或历史记录——你要精确构造它们所需的一切。这样也能为你自己保留用于协调工作的上下文。

**核心原则：** 每个任务一个全新子智能体 + 两阶段审查（先规格后质量）= 高质量、快速迭代

## 流程

\`\`\`dot
digraph process {
    rankdir=TB;

    subgraph cluster_per_task {
        label="每个任务";
        "分派实现子智能体 (code-generator)" [shape=box];
        "实现子智能体有疑问?" [shape=diamond];
        "回答问题，提供上下文" [shape=box];
        "实现子智能体实现、测试、提交、自审" [shape=box];
        "分派规格审查子智能体 (spec-reviewer)" [shape=box];
        "规格审查子智能体确认代码匹配规格?" [shape=diamond];
        "实现子智能体修复规格差距" [shape=box];
        "分派代码质量审查子智能体 (code-quality-reviewer)" [shape=box];
        "代码质量审查子智能体通过?" [shape=diamond];
        "实现子智能体修复质量问题" [shape=box];
        "在 TodoWrite 中标记任务完成" [shape=box];
    }

    "读取计划，提取所有任务的完整文本，记录上下文，创建 TodoWrite" [shape=box];
    "还有剩余任务?" [shape=diamond];
    "分派最终代码审查子智能体审查整体实现" [shape=box];
    "报告完成，验证测试通过，等待用户指示" [shape=box style=filled fillcolor=lightgreen];

    "读取计划，提取所有任务的完整文本，记录上下文，创建 TodoWrite" -> "分派实现子智能体 (code-generator)";
    "分派实现子智能体 (code-generator)" -> "实现子智能体有疑问?";
    "实现子智能体(code-generator)有疑问?" -> "回答问题，提供上下文" [label="是"];
    "回答问题，提供上下文" -> "分派实现子智能体 (code-generator)";
    "实现子智能体有疑问?" -> "实现子智能体实现、测试、提交、自审" [label="否"];
    "实现子智能体实现、测试、提交、自审" -> "分派规格审查子智能体 (spec-reviewer)";
    "分派规格审查子智能体 (spec-reviewer)" -> "规格审查子智能体确认代码匹配规格?";
    "规格审查子智能体确认代码匹配规格?" -> "实现子智能体修复规格差距" [label="否"];
    "实现子智能体修复规格差距" -> "分派规格审查子智能体 (spec-reviewer)" [label="重新审查"];
    "规格审查子智能体确认代码匹配规格?" -> "分派代码质量审查子智能体 (code-quality-reviewer)" [label="是"];
    "分派代码质量审查子智能体 (code-quality-reviewer)" -> "代码质量审查子智能体通过?";
    "代码质量审查子智能体通过?" -> "实现子智能体修复质量问题" [label="否"];
    "实现子智能体修复质量问题" -> "分派代码质量审查子智能体 (code-quality-reviewer)" [label="重新审查"];
    "代码质量审查子智能体通过?" -> "在 TodoWrite 中标记任务完成" [label="是"];
    "在 TodoWrite 中标记任务完成" -> "还有剩余任务?";
    "还有剩余任务?" -> "分派实现子智能体 (code-generator)" [label="是"];
    "还有剩余任务?" -> "分派最终代码审查子智能体审查整体实现" [label="否"];
    "分派最终代码审查子智能体审查整体实现" -> "报告完成，验证测试通过，等待用户指示";
}
\`\`\`

## 处理实现者状态

实现子智能体报告四种状态之一。根据每种状态进行相应处理：

**DONE：** 进入规格合规性审查。

**DONE_WITH_CONCERNS：** 实现者完成了工作但标记了疑虑。在继续之前阅读这些疑虑。如果疑虑涉及正确性或范围，在审查前解决。如果只是观察性说明（如"这个文件越来越大了"），记录下来并继续审查。

**NEEDS_CONTEXT：** 实现者需要未提供的信息。提供缺失的上下文并重新分派。

**BLOCKED：** 实现者无法完成任务。评估阻塞原因：
1. 如果是上下文问题，提供更多上下文并用同一模型重新分派
2. 如果任务需要更强的推理能力，用更强的模型重新分派
3. 如果任务太大，拆分为更小的部分
4. 实施阶段为无人值守，只有在反复尝试仍无法通过时才暂停报告状态（最低优先级）

**绝不** 忽略上报或在不做任何更改的情况下让同一模型重试。如果实现者说卡住了，说明有什么东西需要改变。

## 示例工作流

\`\`\`
你：我正在使用子智能体驱动开发来执行这个计划。

[一次性读取任务文件：task.md]
[提取全部 5 个任务的完整文本和上下文]
[用所有任务创建 TodoWrite]

任务 1：Hook 安装脚本，包含4个子步骤

[获取任务 1 的文本和上下文（已提取）]
[将任务的4个子步骤一次性分派实现子智能体，附带完整任务文本 + 上下文]

实现者："在我开始之前——hook 应该安装在用户级别还是系统级别？"

你："用户级别（~/.config/superpowers/hooks/）"

实现者："明白了。现在开始实现……"
[稍后] 实现者：
  - 实现了 install-hook 命令
  - 添加了测试，5/5 通过
  - 自审：发现遗漏了 --force 参数，已添加
  - 已提交

[分派规格合规审查]
规格审查者：✅ 符合规格 - 所有需求已满足，无多余内容

[获取 git SHA，分派代码质量审查]
代码审查者：优点：测试覆盖好，代码整洁。问题：无。通过。

[标记任务 1 完成]

任务 2：恢复模式

[获取任务 2 的文本和上下文（已提取）]
[分派实现子智能体，附带完整任务文本 + 上下文]

实现者：[无疑问，直接开始]
实现者：
  - 添加了 verify/repair 模式
  - 8/8 测试通过
  - 自审：一切正常
  - 已提交

[分派规格合规审查]
规格审查者：❌ 问题：
  - 缺失：进度报告（规格要求"每 100 项报告一次"）
  - 多余：添加了 --json 参数（未被要求）

[实现者修复问题]
实现者：移除了 --json 参数，添加了进度报告

[规格审查者再次审查]
规格审查者：✅ 现在符合规格

[分派代码质量审查]
代码审查者：优点：扎实。问题（重要）：魔法数字（100）

[实现者修复]
实现者：提取了 PROGRESS_INTERVAL 常量

[代码审查者再次审查]
代码审查者：✅ 通过

[标记任务 2 完成]

...

[所有任务完成后]
[分派最终代码审查]
最终审查者：所有需求已满足，可以合并

完成！
\`\`\`

## 优势

**与手动执行相比：**
- 子智能体自然遵循 TDD
- 每个任务全新上下文（不会混淆）
- 并行安全（子智能体不会互相干扰）
- 子智能体可以提问（工作前和工作中都可以）

**效率提升：**
- 无文件读取开销（控制者提供完整文本）
- 控制者精确策划所需上下文
- 子智能体预先获得完整信息
- 问题在工作开始前就被提出（而非工作结束后）

**质量关卡：**
- 自审在交接前发现问题
- 两阶段审查：规格合规性，然后代码质量
- 审查循环确保修复确实有效
- 规格合规防止过度/不足构建
- 代码质量确保实现良好，验证编译通过

**成本：**
- 更多子智能体调用（每个任务需要实现者 + 2 个审查者）
- 控制者需要更多准备工作（预先提取所有任务）
- 审查循环增加迭代次数
- 但能及早发现问题（比后期调试更省成本）

## 红线

**绝不：**
- 未经用户明确同意就在 main/master 分支上开始实现
- 跳过审查（规格合规性或代码质量）
- 带着未修复的问题继续
- 并行分派多个实现子智能体（会冲突）
- 让子智能体读取计划文件（应提供完整文本）
- 跳过场景铺设上下文（子智能体需要理解任务在哪个环节）
- 忽视子智能体的问题（在让它们继续之前先回答）
- 在规格合规性上接受"差不多就行"（规格审查者发现问题 = 未完成）
- 跳过审查循环（审查者发现问题 = 实现者修复 = 再次审查）
- 让实现者的自审替代正式审查（两者都需要）
- **在规格合规性审查通过之前开始代码质量审查**（顺序错误）
- 在任一审查有未解决问题时就进入下一个任务

**如果子智能体提问：**
- 清晰完整地回答
- 必要时提供额外上下文
- 不要催促它们进入实现阶段

**如果审查者发现问题：**
- 实现者（同一子智能体）修复
- 审查者再次审查
- 重复直到通过
- 不要跳过重新审查

**如果子智能体失败：**
- 分派修复子智能体并提供具体指令
- 不要尝试手动修复（上下文污染）

**子智能体步骤执行纪律：**
- **严格按步骤顺序** - 实现者子智能体必须严格按计划中的步骤顺序执行，不合并、不跳过、不重排
- **TDD 由计划驱动** - 如果计划中某个任务的步骤要求先写测试再写实现，子智能体必须遵循该顺序。TDD 的可靠性取决于计划中是否包含完整的 TDD 步骤`;

export const implementerPromptContent = `# 实现子智能体提示词模板

分派实现子智能体时使用此模板。

\`\`\`
Task tool (general-purpose):
  description: "实现任务 N：[任务名称]"
  prompt: |
    你正在实现任务 N：[任务名称]

    ## 任务描述

    [计划中任务的完整文本 - 粘贴到这里，不要让子智能体去读文件]

    ## 上下文

    [场景铺设：这个任务在哪个环节、依赖关系、架构上下文]

    ## 开始之前

    如果你对以下内容有疑问：
    - 需求或验收标准
    - 方案或实现策略
    - 依赖或假设
    - 任务描述中任何不清楚的地方

    **现在就问。** 在开始工作之前提出任何疑虑。

    ## 你的工作

    当你确认需求清晰后：
    1. 严格按照任务指定的内容实现
    2. 编写测试（如果任务要求则遵循 TDD）
    3. 验证实现是否正常工作
    4. 提交你的工作
    5. 自审（见下文）
    6. 汇报

    工作目录：[directory]

    ## 步骤执行纪律

    严格按计划中的步骤顺序执行。不要合并、跳过或重新排序步骤。
    如果步骤要求"先编写失败的测试"，你必须先写测试再写实现代码。
    步骤顺序是计划作者精心设计的，违反顺序会导致质量问题。

    **工作过程中：** 如果遇到意料之外或不清楚的情况，**提问**。
    随时可以暂停并澄清。不要猜测或做假设。

    ## 代码组织

    你在能一次性放入上下文的代码上推理效果最好，文件聚焦时编辑也更可靠。
    请牢记：
    - 遵循计划中定义的文件结构
    - 每个文件应有单一明确的职责和定义清晰的接口
    - 如果你正在创建的文件超出了计划预期的规模，停下来并以
      DONE_WITH_CONCERNS 状态报告——不要在没有计划指导的情况下自行拆分文件
    - 如果你正在修改的现有文件已经很大或很混乱，小心操作
      并在报告中将其标注为疑虑
    - 在已有代码库中，遵循已建立的模式。像一个好的开发者那样
      改善你接触的代码，但不要重构你任务范围之外的东西。

    ## 当你力不从心时

    说"这对我来说太难了"完全没问题。劣质的工作比不做更糟。
    上报不会受到惩罚。

    **遇到以下情况时停下来上报：**
    - 任务需要在多个有效方案之间做架构决策
    - 你需要理解提供内容之外的代码但找不到答案
    - 你对自己的方案是否正确感到不确定
    - 任务涉及计划未预期的现有代码重构
    - 你一直在逐个读文件试图理解系统但没有进展

    **如何上报：** 以 BLOCKED 或 NEEDS_CONTEXT 状态汇报。具体描述
    你卡在哪里、尝试了什么、需要什么帮助。
    控制者可以提供更多上下文、用更强的模型重新分派，
    或将任务拆分为更小的部分。

    ## 汇报前：自审

    用全新的视角审查你的工作。问自己：

    **完整性：**
    - 我是否完全实现了规格中的所有内容？
    - 我是否遗漏了任何需求？
    - 是否有我没处理的边界情况？

    **质量：**
    - 这是我最好的工作吗？
    - 命名是否清晰准确（匹配事物做什么，而非怎么做）？
    - 代码是否整洁且可维护？

    **纪律：**
    - 我是否避免了过度构建（YAGNI）？
    - 我是否只构建了被要求的内容？
    - 我是否遵循了代码库中的已有模式？

    **测试：**
    - 测试是否真正验证了行为（而非只是 mock 行为）？
    - 我是否严格按计划步骤顺序执行了？如果步骤要求先写测试，我是否先写了测试？
    - 测试是否全面？

    如果在自审中发现问题，在汇报前就修复。

    ## 汇报格式

    完成后汇报：
    - **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
    - 你实现了什么（或尝试了什么，如果被阻塞）
    - 你测试了什么以及测试结果
    - 修改了哪些文件
    - 自审发现（如果有）
    - 任何问题或疑虑

    如果你完成了工作但对正确性有疑虑，使用 DONE_WITH_CONCERNS。
    如果你无法完成任务，使用 BLOCKED。如果你需要
    未提供的信息，使用 NEEDS_CONTEXT。绝不默默产出你不确定的工作。
\`\`\``;

export const specReviewerPromptContent = `# 规格合规审查者提示词模板

分派规格合规审查子智能体时使用此模板。

**目的：** 验证实现者是否构建了所要求的内容（不多不少）

\`\`\`
Task tool (general-purpose):
  description: "审查任务 N 的规格合规性"
  prompt: |
    你正在审查一个实现是否与其规格匹配。

    ## 要求的内容

    [任务需求的完整文本]

    ## 实现者声称构建了什么

    [来自实现者的报告]

    ## 规格文档（如果存在 SPEC.md）

    **SPEC.md 路径：** [SPEC_FILE_PATH]

    逐一检查 SPEC.md 中的每个 WHEN/THEN 场景：
    - 实现是否覆盖了该场景？
    - 场景的预期结果（THEN）是否在代码中得到满足？

    ## 关键：不要信任报告

    实现者完成得疑似过快。他们的报告可能不完整、
    不准确或过于乐观。你必须独立验证所有内容。

    **不要：**
    - 相信他们关于实现内容的说法
    - 信任他们关于完整性的声明
    - 接受他们对需求的解读

    **要做的：**
    - 阅读他们写的实际代码
    - 逐行对比实际实现和需求
    - 检查他们声称已实现但实际遗漏的部分
    - 寻找他们未提及的多余功能

    ## 你的工作

    阅读实现代码并验证：

    **缺失的需求：**
    - 他们是否实现了所有被要求的内容？
    - 是否有他们跳过或遗漏的需求？
    - 是否有他们声称可用但实际未实现的功能？
    - （如果有 SPEC.md）每个 WHEN/THEN 场景是否都被覆盖？

    **TODO/FIXME 标记：**
    - 扫描实现代码中的 TODO、FIXME、HACK、XXX 等标记
    - 如果标记涉及当前任务要求实现的功能，标记为缺失——TODO 不是实现，而是承认未完成
    - 如果标记涉及当前任务范围之外的未来工作，可忽略

    **多余/不需要的工作：**
    - 他们是否构建了未被要求的内容？
    - 他们是否过度工程化或添加了不必要的功能？
    - 他们是否添加了规格中没有的"锦上添花"功能？

    **理解偏差：**
    - 他们是否以不同于预期的方式解读了需求？
    - 他们是否解决了错误的问题？
    - 他们是否实现了正确的功能但方式不对？

    **通过阅读代码来验证，而非信任报告。**

    报告：
    - ✅ 符合规格（如果经过代码检查后一切匹配）
    - ❌ 发现问题：[具体列出缺失或多余的内容，附带 file:line 引用]
\`\`\``;

export const codeQualityReviewerPromptContent = `# 代码质量审查者提示词模板

分派代码质量审查子智能体时使用此模板。

**目的：** 验证实现可以编译且所有测试通过

**仅在规格合规性审查通过后才分派。**

\`\`\`
Task tool (general-purpose):
  description: "验证任务 N 的编译和测试"
  prompt: |
    你正在验证一个实现的基本质量门禁。

    ## 任务摘要

    [任务摘要]

    ## Git 范围

    Base: [BASE_SHA]
    Head: [HEAD_SHA]

    ## 你的工作

    **仅执行以下两项检查，不做其他任何事：**

    1. **编译检查：** 运行项目的编译/构建命令，确认无编译错误
    2. **测试检查：** 运行项目的单元测试命令，确认所有测试通过

    常用命令（根据项目类型选择）：
    - TypeScript/JavaScript: \`npm run build\` 和 \`npm test\`
    - Python: 无显式编译步骤，运行 \`pytest\` 或 \`python -m pytest\`
    - Go: \`go build ./...\` 和 \`go test ./...\`
    - Java: \`mvn compile\` 和 \`mvn test\`
    - Rust: \`cargo build\` 和 \`cargo test\`

    如果不确定项目的构建/测试命令，检查 package.json、Makefile、
    Cargo.toml、pom.xml 等项目配置文件。

    ## 报告格式

    报告：
    - **编译：** ✅ 通过 / ❌ 失败（附错误信息）
    - **测试：** ✅ 通过 / ❌ 失败（附失败测试名称和错误信息）
    - **结论：** 通过（两者均通过）/ 不通过（任一失败）
\`\`\`

**代码审查者返回：** 编译状态、测试状态、结论（通过/不通过）`;

export function getSubagentDrivenDevelopmentSkillTemplate(): SkillTemplate {
  return {
    name: 'subagent-driven-development',
    description: 'subagent-driven-development',
    instructions: sddInstructions,
    license: 'MIT',
    compatibility: '无特殊依赖。',
    metadata: { author: 'superpowers', version: '1.0' },
  };
}
