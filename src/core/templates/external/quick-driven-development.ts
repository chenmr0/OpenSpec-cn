/**
 * Quick-Driven Development External Skill Template
 *
 * A lightweight alternative to subagent-driven-development.
 * It keeps per-task implementation subagents, but removes the per-task
 * two-stage spec compliance and code quality review flow.
 */
import type { SkillTemplate } from '../types.js';

const quickInstructions = `# 子智能体驱动开发

通过为每个任务分派一个全新的子智能体来执行计划。任务完成后跳过两阶段审查，直接根据实现者的验证结果和自审报告标记完成并进入下一个任务。

**为什么用子智能体：** 你将任务委派给具有隔离上下文的专用智能体。通过精心设计它们的指令和上下文，确保它们专注并成功完成任务。它们不应继承你的会话上下文或历史记录——你要精确构造它们所需的一切。这样也能为你自己保留用于协调工作的上下文。

**核心原则：** 每个任务一个全新子智能体 + 实现者验证和自审 = 轻量、快速迭代

## 流程

\`\`\`dot
digraph process {
    rankdir=TB;

    subgraph cluster_per_task {
        label="每个任务";
        "分派实现子智能体 (code-generator)" [shape=box];
        "实现子智能体有疑问?" [shape=diamond];
        "回答问题，提供上下文" [shape=box];
        "实现子智能体实现、测试、提交、自审" [shape=box];
        "在 TodoWrite 中标记任务完成，更新 task.md 复选框 [ ] → [x]" [shape=box];
    }

    "读取计划，提取所有任务的完整文本，记录上下文，创建 TodoWrite" [shape=box];
    "还有剩余任务?" [shape=diamond];
    "分派最终代码审查子智能体审查整体实现" [shape=box];
    "报告完成，验证测试通过，等待用户指示" [shape=box style=filled fillcolor=lightgreen];

    "读取计划，提取所有任务的完整文本，记录上下文，创建 TodoWrite" -> "分派实现子智能体 (code-generator)";
    "分派实现子智能体 (code-generator)" -> "实现子智能体有疑问?";
    "实现子智能体(code-generator)有疑问?" -> "回答问题，提供上下文" [label="是"];
    "回答问题，提供上下文" -> "分派实现子智能体 (code-generator)";
    "实现子智能体有疑问?" -> "实现子智能体实现、测试、提交、自审" [label="否"];
    "实现子智能体实现、测试、提交、自审" -> "在 TodoWrite 中标记任务完成，更新 task.md 复选框 [ ] → [x]";
    "在 TodoWrite 中标记任务完成，更新 task.md 复选框 [ ] → [x]" -> "还有剩余任务?";
    "还有剩余任务?" -> "分派实现子智能体 (code-generator)" [label="是"];
    "还有剩余任务?" -> "分派最终代码审查子智能体审查整体实现" [label="否"];
    "分派最终代码审查子智能体审查整体实现" -> "报告完成，验证测试通过，等待用户指示";
}
\`\`\`

## 处理实现者状态

实现子智能体报告四种状态之一。根据每种状态进行相应处理：

**DONE：** 记录实现者的修改文件、验证结果和自审结论，然后在 TodoWrite 中标记任务完成。

**DONE_WITH_CONCERNS：** 实现者完成了工作但标记了疑虑。在继续之前阅读这些疑虑。如果疑虑涉及正确性或范围，先让实现者修复。如果只是观察性说明（如"这个文件越来越大了"），记录下来并标记任务完成。

**NEEDS_CONTEXT：** 实现者需要未提供的信息。提供缺失的上下文并重新分派。

**BLOCKED：** 实现者无法完成任务。评估阻塞原因：
1. 如果是上下文问题，提供更多上下文并用同一模型重新分派
2. 如果任务需要更强的推理能力，用更强的模型重新分派
3. 如果任务太大，拆分为更小的部分
4. 实施阶段为无人值守，只有在反复尝试仍无法通过时才暂停报告状态（最低优先级）

**绝不** 忽略上报或在不做任何更改的情况下让同一模型重试。如果实现者说卡住了，说明有什么东西需要改变。

## 示例工作流

\`\`\`
你：我正在使用轻量子智能体驱动开发来执行这个计划。

[一次性读取任务文件：task.md]
[提取全部 5 个任务的完整文本和上下文]
[用所有任务创建 TodoWrite]

任务 1：Hook 安装脚本，包含4个子步骤

[获取任务 1 的文本和上下文（已提取）]
[将任务的4个子步骤一次性分派实现子智能体，附带完整任务文本 + 上下文]

实现者："在我开始之前——hook 应该安装在用户级别还是系统级别？"

你："用户级别（~/.config/superpowers/hooks/）"

实现者："明白了。现在开始实现……"
[稍后] 实现者：
  - 实现了 install-hook 命令
  - 添加了测试，5/5 通过
  - 自审：发现遗漏了 --force 参数，已添加
  - 已提交
  - 状态：DONE

[标记任务 1 完成，更新 task.md 中 ### [ ] → ### [x]]

任务 2：恢复模式

[获取任务 2 的文本和上下文（已提取）]
[分派实现子智能体，附带完整任务文本 + 上下文]

实现者：[无疑问，直接开始]
实现者：
  - 添加了 verify/repair 模式
  - 8/8 测试通过
  - 自审：发现魔法数字 100，已提取 PROGRESS_INTERVAL 常量
  - 已提交
  - 状态：DONE

[标记任务 2 完成，更新 task.md 中 ### [ ] → ### [x]]

...

[所有任务完成后]
[分派最终代码审查]
最终审查者：所有需求已满足，可以合并

完成！
\`\`\`

## 优势

**与手动执行相比：**
- 子智能体自然遵循 TDD
- 每个任务全新上下文（不会混淆）
- 串行安全（避免多个实现子智能体互相冲突）
- 子智能体可以提问（工作前和工作中都可以）

**效率提升：**
- 无文件读取开销（控制者提供完整文本）
- 控制者精确策划所需上下文
- 子智能体预先获得完整信息
- 问题在工作开始前就被提出（而非工作结束后）

**质量关卡：**
- 实现者自审在交接前发现问题
- 实现者运行任务要求的验证命令
- 控制者检查实现者报告中的验证结果和疑虑
- 变更级验证在所有任务完成后统一执行

**成本：**
- 每个任务只需要实现者子智能体
- 跳过规格合规审查和代码质量审查两个 per-task 子智能体调用
- 没有审查循环，迭代次数更少
- 代价是缺少每任务独立审查，适合低风险变更

## 红线

**绝不：**
- 未经用户明确同意就在 main/master 分支上开始实现
- 分派规格审查子智能体（spec-reviewer）
- 分派代码质量审查子智能体（code-quality-reviewer）
- 带着未修复的问题继续
- 并行分派多个实现子智能体（会冲突）
- 让子智能体读取计划文件（应提供完整文本）
- 跳过场景铺设上下文（子智能体需要理解任务在哪个环节）
- 忽视子智能体的问题（在让它们继续之前先回答）
- 在实现者未验证或自审前就进入下一个任务
- 在实现者报告 BLOCKED、NEEDS_CONTEXT 或重要疑虑时标记任务完成

**如果子智能体提问：**
- 清晰完整地回答
- 必要时提供额外上下文
- 不要催促它们进入实现阶段

**如果子智能体失败：**
- 分派修复子智能体并提供具体指令
- 不要尝试手动修复（上下文污染）

**子智能体步骤执行纪律：**
- **严格按步骤顺序** - 实现者子智能体必须严格按计划中的步骤顺序执行，不合并、不跳过、不重排
- **TDD 由计划驱动** - 如果计划中某个任务的步骤要求先写测试再写实现，子智能体必须遵循该顺序。TDD 的可靠性取决于计划中是否包含完整的 TDD 步骤`;

export function getQuickDrivenDevelopmentSkillTemplate(): SkillTemplate {
  return {
    name: 'quick-driven-development',
    description: '轻量任务实现流程：每任务实现子代理顺序执行，跳过两阶段子代理审查。',
    instructions: quickInstructions,
    license: 'MIT',
    compatibility: '无特殊依赖。',
    metadata: { author: 'codespec', version: '1.0' },
  };
}
