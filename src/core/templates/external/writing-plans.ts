/**
 * Writing Plans External Skill Template
 *
 * Copied from superpowers-cn/skills/writing-plans.
 * This skill is always installed during init alongside command-only workflows.
 */
import type { SkillTemplate } from '../types.js';

const writingPlansInstructions = `# 编写计划

## 概述

编写精简、可执行的实现计划，假设执行者对当前代码库上下文有限。计划只回答 **DO**：要修改哪些文件、按什么顺序完成哪些任务、每个任务如何验证。DRY。YAGNI。

计划文档不是代码生成文档，不提前替实现者编写代码。除非存在复杂算法、状态机或协议编排，且不用伪代码会导致实现歧义，否则禁止在计划中写伪代码。

假设执行者是有经验的开发者，但不熟悉本项目的工具链和问题领域。

**开始时宣布：** "我正在使用 writing-plans 技能创建实现计划。"

**计划保存位置：** \`codespec/changes/<name>/task.md\`
- （用户对计划位置的偏好优先于此默认值）

## 任务划分原则

在定义任务之前，先列出将要创建或修改的文件以及每个文件的职责。这是锁定分解决策的地方。

- 每个任务对应一个可独立验证的业务能力或技术闭环。
- 优先按业务能力/状态流/接口契约划分，不按“一个函数一个任务”划分。
- 每个任务引用相关 spec.md 业务规则、验收条件或异常场景即可，不重复全文。
- 每个任务引用相关 design.md 设计决策、接口变更、数据结构变更或流程设计即可，不重复设计说明。
- 任务内部可以包含多个文件变更，但必须形成一个可测试、可审查的完整交付单元。
- 在现有代码库中，遵循已有模式。如果代码库使用大文件，不要单方面重构——但如果你正在修改的文件已经变得难以管理，在计划中包含拆分是合理的。

## 任务粒度

**推荐总任务数 3–6 个；小变更可以更少。每个任务覆盖 2–5 个验收场景，涉及 1–5 个主要文件，能够在一次实现上下文中完成。**

**选择 TDD 时（严格红-绿-重构，每个任务必须包含以下步骤）：**
- "编写失败的测试" - 一步
- "运行它确认失败" - 一步
- "实现最少代码让测试通过" - 一步
- "运行测试确认通过" - 一步
- "编译检查" - 一步
- "Commit" - 一步

**未选择 TDD 时（每个任务仍必须包含测试验证）：**
- "实现功能代码" - 一步
- "编译检查" - 一步
- "Commit" - 一步

## 计划文档头部

**每个计划必须以此头部开始：**

\`\`\`markdown
# [功能名称] 实现计划

> 任务标题使用复选框（\`### [ ]\`）语法来跟踪进度，步骤使用（\`-\`）语法记录详细操作。

**目标：** [一句话说明本次变更要达成的业务/技术结果]

---

## 1. 职责边界
- 禁止为了完成本次需求进行无关重构。
- 禁止修改未列入计划的核心文件；如必须修改，先在任务回传中说明原因。
- 遵循现有代码风格、错误码风格、日志风格和测试组织方式。
- 如果发现更合适的文件归属，必须说明依据，不得直接大规模迁移。

---
\`\`\`

## 任务结构

> **注意：** 以下模板展示了 TDD 格式的任务步骤。如果用户选择了 TDD，每个任务必须严格遵循此模板（先写失败测试，再写实现）。如果未选择 TDD，则跳过测试步骤。

\`\`\`\`markdown
### [ ] 任务 N：[任务名称：建议使用“业务能力 + 技术动作”命名]

**任务目标：**
[说明本任务要完成的完整能力。不要写实现代码。重点说明输入、处理责任、输出结果、状态变化或错误行为。]

**依据：**
- 规格：\`codespec/changes/<name>/spec.md\` 1.1 规则名称 / 场景名称
- 设计：\`codespec/changes/<name>/design.md\` 2.1 决策 / 设计名称

**涉及文件：**
| [文件名] | 修改 | [说明该文件承担的职责]
| [文件名] | 创建 | [说明新增模块职责]
| [文件名] | 创建/修改 | [说明验证哪些业务场景]

**修改入口：**
- \`[函数 / 类 / 方法 / 状态机节点 / 规则判断入口]\`

**执行步骤：**
- **步骤 1：读取上下文**
   - 读取本任务关联的 spec/design 章节。
   - 读取涉及文件中的真实代码。
   - 确认入口、类型、错误码、工具函数是否真实存在。

- **步骤 2：编写失败的测试**：\`[测试文件路径]\`

- **步骤 3：运行测试验证失败**：\`[精确测试命令]\`
   - 预期：FAIL，原因为 \`[预期失败原因]\`

- **步骤 4：按实现约束完成最小实现**：
   - 这是实现阶段的执行动作，不在 task.md 中展开实现代码或完整伪代码。
   - 默认以“涉及文件”作为修改边界。
   - 按实现约束完成行为，不做无关重构。
   - 不得新增 task/spec/design 中没有依据的新能力。

- **步骤 5：运行测试验证通过**：\`[精确测试命令]\`
   - 预期：PASS

- **步骤 6：Commit**
   - 指定你的修改文件提交，示例：
   \`\`\`bash
   git add tests/path/test.py src/path/file.py
   git commit -m "feat: add specific feature"
   \`\`\`
\`\`\`\`

## 禁止占位符

每个步骤都必须包含工程师需要的实际内容。以下是**计划缺陷**——绝不要写出来：
- "待定"、"TODO"、"后续实现"、"补充细节"
- "添加适当的错误处理" / "添加验证" / "处理边界情况"
- "类似任务 N"（重复代码——工程师可能不按顺序阅读任务）
- 引用了未在任何任务中定义的类型、函数或方法
- 大段复制 spec.md 的验收条件、design.md 的设计说明或方案对比

## 注意事项
- 始终使用精确的文件路径
- 精确的命令和预期输出
- DRY、YAGNI、TDD、 commit

## 自检

编写完整计划后，以全新视角审视规格并对照检查计划。这是你自己执行的检查清单——不是子代理调度。

**1. 规格覆盖度：** 浏览 spec.md 中的每个需求和 WHEN/THEN 场景。你能指出实现它的任务吗？检查每个 WHEN/THEN 场景是否在计划中有对应的测试或验证步骤。列出所有遗漏。

**2. 占位符扫描：** 搜索计划中的红旗——上方"禁止占位符"章节中的任何模式。修复它们。

**3. 类型一致性：** 后续任务中使用的类型、方法签名和属性名是否与前面任务中定义的一致？任务 3 中叫 \`clearLayers()\` 但任务 7 中叫 \`clearFullLayers()\` 就是 bug。

**4. 测试步骤完整性：** 如果用户选择了 TDD，验证每个任务都包含"编写失败的测试"和"运行测试验证失败"步骤。如果未选择 TDD，则跳过。

如果发现问题，直接内联修复。无需重新审查——修好继续推进。如果发现规格中的需求没有对应任务，就添加任务。

## 执行交接

保存计划后，提供简短交接：
"计划已完成并保存到 \`codespec/changes/<name>/task.md\`。"`;

export function getWritingPlansSkillTemplate(): SkillTemplate {
  return {
    name: 'writing-plans',
    description: 'writing-plans',
    instructions: writingPlansInstructions,
    license: 'MIT',
    compatibility: '无特殊依赖。',
    metadata: { author: 'superpowers', version: '1.0' },
  };
}
