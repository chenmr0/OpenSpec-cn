/**
 * Design Workflow Template
 *
 * Refines a rough AR draft into a complete architecture requirement document.
 */
import type { SkillTemplate, CommandTemplate } from '../types.js';

const designInstructions = `# 架构需求设计：完善 AR 初稿

将简略的架构需求描述完善为结构化 AR 文档。该流程专注于需求背景、变更内容、实现约束与线索，并将所有澄清问答集中记录到最后的参考信息章，为后续 \`/codespec/plan\` 生成 \`design.md\` 和 \`task.md\` 提供输入。

<HARD-GATE>
本流程只产出 \`ar.md\`。不要创建 \`design.md\`、\`task.md\`，也不要修改业务代码。
</HARD-GATE>

<WRITE-GATE>
每批用户澄清回复后，必须先编辑并保存 \`ar.md\`，再进入下一批问题或下一阶段。不要把需求、测试、实现澄清全部问完后再做一次总编辑。
</WRITE-GATE>

## 检查清单

你必须为以下每个条目创建任务，Todo跟踪，并按顺序完成：

1. 创建变更目录及AR文件
2. 填充需求背景，填写对应章节
3. 业务概念澄清，填写对应章节
4. 需求与测试澄清，填写对应章节
5. 实现澄清，填写对应章节

## 流程详述

1. **输入**：用户的请求应包含变更名称（kebab-case）或对他们想要构建内容的描述。

**如果没有提供明确的输入，询问他们想要构建什么**

   使用 **AskUserQuestion tool**（开放式，无预设选项）询问：
   > "您想要处理什么变更？请描述您想要构建或修复的内容。"

   根据他们的描述，推导出一个 kebab-case 名称（例如："add user authentication" → \`add-user-auth\`）。

   **重要提示**：在不了解用户想要构建什么的情况下，请勿继续。

2. **创建变更目录及AR文件**
   \`\`\`bash
   codespec new change "<name>"
   \`\`\`
   \`\`\`bash
   codespec create ar --change "<name>" --json
   \`\`\`
   这将在 \`codespec/changes/<name>/\` 创建一个AR模板脚手架 \`ar.md\`。读取命令返回的 \`filePath\`，后续只编辑这个 \`ar.md\`。

3. 填充需求背景

从原始 AR 描述中提取并写入：
- \`1.1 业务痛点\`：当前不足、用户痛点、核心问题
- \`1.2 业务目标\`：业务驱动、期望价值、受益对象

规则：
- 只从原始描述和用户明确澄清中提取
- 不凭空补充背景或目标
- 保留关键原始措辞

4. 业务概念澄清

1. 识别原始 AR 中需要澄清的业务概念、领域术语、组织内部缩写。
2. 检查当前可用工具中是否有豆包 MCP 工具。
3. 如果有，调用 \`concept-clarify\` agent，传入问题列表和 \`ar.md\` 路径，让它只填写 \`4.1 业务背景知识/概念澄清\`。
4. 如果没有豆包 MCP，跳过 agent 调用，在第 4.1 节保留待澄清问题和 \`A：<!-- 待澄清 -->\`。

5. 需求与测试澄清

以 SE / TSE 视角完善第 2 章，并将问答记录写入第 4 章：
- 功能清单：功能 ID、名称、优先级、一句话描述
- 不在范围内：明确排除项和原因
- 关键场景：功能点 / Given / When / Then，只写业务可观察行为
- \`4.2 需求澄清\`：业务规则、范围边界、异常场景的问答记录
- \`4.3 测试澄清\`：验收标准、边界条件、可测试性的问答记录

工作方式：
- 先做最小代码探索，形成候选理解，再向用户确认关键不确定点
- 按“提问策略”批量提出关键问题，每批不超过 8 个
- 优先使用选择题，并给出具体推荐选项和原因
- 当两类视角都没有新的关键问题，或用户要求停止时，写入第 2 章，并把问答记录写入第 4.2、4.3 节

6. 实现澄清

以开发 / MDE 视角完善第 3 章，并将问答记录写入第 4.4 节：
- 定位相关模块、已有接口、已有数据结构、测试位置或文档链接
- 记录明确的设计约束 / DFX 约束，如兼容性、可靠性、性能、安全、可观测性、交付限制等
- 记录相关代码 / 流程线索，只写定位和事实，不在 AR 中完成详细技术设计
- 如已形成明确方向，记录候选方案、取舍或后续 design.md 需要重点展开的问题
- \`4.4 实现澄清\`：架构主张、模块边界、接口对齐、代码落点的问答记录

规则：
- 所有实现线索和约束必须基于已有代码、文档或澄清记录
- 不展开完整接口设计、数据结构设计或流程设计，这些会在其他流程处理
- 不做与当前目标无关的重构建议
- 需要用户决策的实现问题同样按“提问策略”批量提出，每批不超过 8 个

7. 完成

\`codespec/changes/<name>/ar.md\` 至少包含：
- 第 1 章：需求背景
- 第 2 章：变更内容
- 第 3 章：实现约束与线索
- 第 4 章：参考信息（业务概念、需求、测试、实现澄清问答）

请用户在继续之前审查ar.md：
> "AR 成稿已写入 \`ar.md\`。请审查一下，如果你想做任何修改，请告诉我。"
使用 **AskUserQuestion tool** 等待用户回复，选项包含"批准","需要修改"。只有在用户批准后才继续。

**输出**
- 准备就绪："AR终稿已完成！"
- 提示："可继续运行 \`/codespec/plan <name>\` 生成 design.md 和 task.md。"

## 提问策略

在需要用户澄清时，优先批量提问，减少多角色反复打断：

1. **先探索再提问**：对不明确的概念、模块、接口或约束，先做最小代码/文档探索，形成候选理解；不要把可以自行查证的问题直接抛给用户。
2. **一次提问多条**：将同一阶段发现的关键问题整理为 \`questions\` 数组一次性发出，而不是多轮逐个提问。
3. **单次上限 8 个问题**：如果发现项超过 8 条，按阻塞程度和影响范围排序，分批提问；优先询问会影响范围、验收标准、技术方向的关键问题。
4. **每个问题提供三个层级选项**：推荐方案 (Recommended) → 备选方案 → 用户自定义输入。若交互工具自动提供自定义输入入口，则显式提供推荐方案和备选方案即可。
5. **推荐方案必须具体**：禁止使用“补充相关内容”“按实际情况处理”等模糊推荐。推荐项必须给出可直接写入 AR 的具体补充文本、修正值、范围边界或验收条件。
6. **问题要可回答**：每个问题只围绕一个决策点，避免把多个无关判断合并到同一个问题里。
7. **批量提问不等于延迟编辑**：批量提问仍按阶段组织，但同阶段批量问答和阶段性编辑不能合并到最后一次总编辑；每批回复后都要立即更新对应章节。

<!-- command: codespec-design -->`;

export function getOpsxDesignSkillTemplate(): SkillTemplate {
  return {
    name: 'codespec-design',
    description: '将简略 AR 初稿完善为结构化架构需求文档 ar.md，为后续 /codespec/plan 提供输入。',
    instructions: designInstructions,
    license: 'MIT',
    compatibility: '需要 codespec CLI。',
    metadata: { author: 'codespec', version: '1.0' },
  };
}

export function getOpsxDesignCommandTemplate(): CommandTemplate {
  return {
    name: 'OPSX: Design',
    description: '完善 AR 初稿 - 需求背景、变更内容、实现约束与澄清记录',
    category: 'Workflow',
    tags: ['workflow', 'ar', 'design'],
    content: designInstructions,
  };
}
