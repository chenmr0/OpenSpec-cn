/**
 * Propose Workflow Template
 *
 * Interactive brainstorming + spec/design/plan workflow.
 * Integrates Superpowers brainstorming and writing-plans approaches
 * into the CodeSpec artifact-driven framework.
 */
import type { SkillTemplate, CommandTemplate } from '../types.js';

const proposeInstructions = `# 头脑风暴：将想法转化为设计

通过自然的协作对话，帮助将想法转化为完整的设计和规格说明。

首先了解当前项目的上下文，然后逐一提问来完善想法。一旦你理解了要构建的内容，就展示设计方案并获得用户批准。

<HARD-GATE>
在你展示设计方案并获得用户批准之前，不要编写任何代码、搭建任何项目或采取任何实现行动。
</HARD-GATE>

## 检查清单

你必须为以下每个条目创建任务，Todo跟踪，并按顺序完成：

1. **探索项目上下文** — 检查文件、文档、最近的 commit
3. **提出澄清问题** — 每次一个，了解目的/约束/成功标准
4. **提出 2-3 种方案** — 附带权衡分析和你的推荐
5. **展示设计** — 按复杂度分节展示，每节展示后获得用户批准
6. **编写规格文档（SPEC.md）** — 提取需求，编写 WHEN/THEN 场景，保存到 \`spec.md\`
7. **规格文档自检** — 场景完整性、可测试性、一致性、范围（详见下方）
8. **用户审查规格文档** — 在继续之前请用户审查 SPEC.md
9. **编写设计文档（DESIGN.md）** — 提取技术决策、架构、风险，保存到 \`design.md\`
10. **设计文档自检** — 需求追溯、占位符、一致性（详见下方）
11. **用户审查设计文档** — 确认后过渡到 writing-plans

## 流程详述

**输入**：用户的请求应包含变更名称（kebab-case）或对他们想要构建内容的描述。

**如果没有提供明确的输入，询问他们想要构建什么**

   使用 **AskUserQuestion tool**（开放式，无预设选项）询问：
   > "您想要处理什么变更？请描述您想要构建或修复的内容。"

   根据他们的描述，推导出一个 kebab-case 名称（例如："add user authentication" → \`add-user-auth\`）。

   **重要提示**：在不了解用户想要构建什么的情况下，请勿继续。

**创建变更目录**
   \`\`\`bash
   codespec new change "<name>"
   \`\`\`
   这将在 \`codespec/changes/<name>/\` 创建一个带有 \`.codespec.yaml\` 的脚手架变更。

**快速探索**

   从用户的描述中提取 3-5 个核心关键词，进行**最多 10 次**工具调用（Glob/Grep），快速定位相关代码。

   探索目标（按需，不是每项必做）：
   - **检查各模块\`DESIGN.md\` 下是否有相关内容，\`DESIGN.md\`包含全量实现设计**
   - 首先查看当前项目状态（文件、文档、最近的 commit）
   - 找到与用户需求最直接相关的现有实现（3-5 个参考文件即可）

   **探索边界**：
   - **不要**读取完整的源文件内容——只需要文件路径和关键片段（使用 Grep 而非 Read）
   - 目标是建立**足够的上下文来提出高质量问题**，而不是完全理解整个代码仓

**理解想法**

   探索和提问可以交替进行。如果探索中发现不清楚的地方，直接提问——不需要等"充分探索完毕"。

   - 在提出详细问题之前，先评估范围：如果需求描述了多个独立子系统（例如"构建一个包含聊天、文件存储、计费和分析的平台"），立即指出这一点。不要花时间用问题去细化一个需要先拆分的项目。
   - 如果项目规模过大，单个规格说明无法覆盖，帮助用户分解为子项目：有哪些独立的部分，它们之间有什么关系，应该按什么顺序构建？然后通过正常的设计流程进行第一个子项目的头脑风暴。每个子项目都有自己的规格 → 计划 → 实现周期。
   - 对于范围适当的项目，每次提一个问题来完善想法
   - 尽量使用选择题，开放式问题也可以
   - 每条消息只提一个问题——如果一个主题需要更多探索，拆分成多个问题
   - 重点理解：目的、约束、成功标准
   
   使用 **AskUserQuestion tool** 提出每个问题。

**提出方案**

   - 提出 2-3 种不同的方案及其权衡
   - 以对话的方式展示选项，附上你的推荐和理由
   - 先展示你推荐的方案并解释原因

**展示设计**

   - 一旦你认为理解了要构建的内容，就展示设计
   - 每个部分的篇幅与其复杂度匹配：简单的几句话，复杂的最多 200-300 字
   - 每个部分展示后询问是否正确
   - 涵盖：架构、组件、数据流、错误处理、测试
   - 随时准备回头澄清不明确的地方

**面向隔离和清晰的设计**

   - 将系统拆分为更小的单元，每个单元有一个明确的职责，通过定义良好的接口通信，可以独立理解和测试
   - 对于每个单元，你应该能回答：它做什么，如何使用，它依赖什么？
   - 别人能否不看内部实现就理解一个单元的功能？你能否在不影响调用者的情况下修改内部实现？如果不能，边界需要调整。
   - 更小、边界清晰的单元也更便于你工作——你对能一次放入上下文的代码推理得更好，文件越专注你的编辑越可靠。当文件变大时，这通常意味着它承担了过多职责。
   
   使用 **AskUserQuestion tool** 获取用户对每节的批准。如果用户要求修改，迭代修改。

**在现有代码库中工作**

   - 在提出更改之前先探索现有结构。遵循现有模式。
   - 如果现有代码存在影响当前工作的问题（例如文件过大、边界不清、职责纠缠），在设计中包含有针对性的改进——就像一个优秀的开发者在工作中改进经手的代码一样。
   - 不要提议无关的重构。专注于服务当前目标的事情。

## 设计之后
### 编写 SPEC.md（规格文档）

   用户批准设计方案后，先提取 **WHAT**（系统应该做什么），编写规格文档：

   a. 获取指令：
   \`\`\`bash
   codespec instructions specs --change "<name>" --json
   \`\`\`

   b. 编写规范文件

   c. **SPEC.md 自检：**：
   编写规格文档后，以全新的视角审视它：

   1. **场景完整性：** 每个需求是否至少有一个 WHEN/THEN 场景？
   2. **可测试性：** 每个场景是否足够具体，可以转化为测试用例？
   3. **内部一致性：** 各需求之间有矛盾吗？
   4. **范围检查：** 这是否聚焦到可以用一个实现计划覆盖，还是需要进一步拆分？
   5. **模糊性检查：** 有没有需求可以被两种方式理解？如果有，选择一种并明确写出来。

   发现问题时直接内联修复。无需重新审查——修好继续推进。

   d. **用户审查 SPEC.md：**：
   自检完成后，请用户在继续之前审查规格文档：

   > "规范文档已写入 \`spec.md\`。请审查一下，如果在我们继续编写设计文档之前你想做任何修改，请告诉我。"

   使用 **AskUserQuestion tool** 等待用户回复。如果他们要求修改，做出修改并重新运行自检。只有在用户批准后才继续。

### 编写 DESIGN.md（设计文档）

   获得用户批准后，提取 **HOW**（系统如何实现），编写技术设计文档：

   a. 获取指令：
   \`\`\`bash
   codespec instructions design --change "<name>" --json
   \`\`\`

   b. 编写技术设计文档

   c. **执行设计自检**：
   编写规格文档后，以全新的视角审视它：
   1. **占位符扫描：** 有没有"待定"、"TODO"、未完成的章节或模糊的需求？修复它们。
   2. **内部一致性：** 各章节之间有矛盾吗？架构和功能描述匹配吗？
   3. **范围检查：** 这是否聚焦到可以用一个实现计划覆盖，还是需要进一步拆分？
   4. **模糊性检查：** 有没有需求可以被两种方式理解？如果有，选择一种并明确写出来。

   发现问题时直接内联修复。无需重新审查——修好继续推进。

   d. **用户审查关卡**：
   规格自检完成后，请用户在继续之前审查书面规格：
   > "设计文档已写入 \`design.md\`。请审查一下，如果在我们开始编写实现计划之前你想做任何修改，请告诉我。"

   使用 **AskUserQuestion tool** 等待用户回复。如果他们要求修改，做出修改并重新运行规格自检。只有在用户批准后才继续。

### 编写  plan.md（计划文档）

    设计获得用户批准后，创建实现计划：

    a. 获取指令：
    \`\`\`bash
    codespec instructions plan --change "<name>" --json
    \`\`\`

    b. **强制使用外部技能创建计划。你必须按顺序执行：**

       **第一步：加载 \`writing-plans\` 技能技能创建详细的实现计划。

       **第二步：使用 **AskUserQuestion tool** 询问用户，是否启用测试驱动开发（TDD）。** 如果用户选择启用，必须立即加载 \`test-driven-development\` 技能。 必须将其应用于 writing-plans，**每个任务必须先写失败测试，再写最少实现**。

       **计划保存位置：** \`codespec/changes/<name>/plan.md\`

    c. **用户审查关卡**：
   计划自检完成后，请用户在继续之前审查书面规格：
   > "计划文档已写入 \`plan.md\`。请审查一下，如果你想做任何修改，请告诉我。"

   使用 **AskUserQuestion tool** 等待用户回复。如果他们要求修改，做出修改并重新运行计划自检。只有在用户批准后才继续。

**显示最终状态**
    \`\`\`bash
    codespec status --change "<name>"
    \`\`\`

**输出**

完成所有产出物后，总结：
- 准备就绪："所有产出物已创建！准备好实现。"
- 提示："运行 \`/sdd/apply\` 开始实现，或者运行 \`/new\` 清空上下文后再 \`/sdd/apply\` 实现。(推荐)"

**产出物创建指南**

- 遵循每个产出物类型的 \`codespec instructions\` 中的 \`instruction\` 字段
- Schema 定义了每个产出物应包含的内容，遵循它
- **不要重新读取已经在本次对话中创建或讨论过的产出物文件**——你已经知道其内容，直接基于对话上下文编写
- 使用 \`template\` 作为输出文件的结构 - 填充其各个部分
- **重要提示**：\`context\` 和 \`rules\` 是对你的约束，而不是文件内容
  - 不要将 \`<context>\`、\`<rules>\`、\`<project_context>\` 块复制到产出物中
  - 这些引导你编写内容，但不应出现在输出中

**护栏**
- 快速探索后即开始提问——不要等"完全理解"才提问，探索和提问可以交替进行
- **优先选择题** — 在可能的情况下比开放式问题更容易回答
- **严格遵循 YAGNI** — 从所有设计中移除不必要的功能
- **探索替代方案** — 在做决定之前始终提出 2-3 种方案
- **增量验证** — 展示设计，获得批准后再继续
- **保持灵活** — 有不明确的地方就回头澄清`;

export function getOpsxProposeSkillTemplate(): SkillTemplate {
  return {
    name: 'codespec-propose',
    description: '通过头脑风暴交互式提案新变更。探索用户意图、提出方案、编写规范和设计文档，最终生成实现计划。在单个命令中完成从需求到计划的完整设计流程。',
    instructions: proposeInstructions,
    license: 'MIT',
    compatibility: '需要 codespec CLI。',
    metadata: { author: 'codespec', version: '2.0' },
  };
}

export function getOpsxProposeCommandTemplate(): CommandTemplate {
  return {
    name: 'OPSX: Propose',
    description: '通过头脑风暴交互式提案新变更 - 探索、设计、规范、计划',
    category: 'Workflow',
    tags: ['workflow', 'artifacts', 'brainstorming'],
    content: proposeInstructions,
  };
}
