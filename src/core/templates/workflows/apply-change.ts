/**
 * Skill Template Workflow Modules
 *
 * This file is generated by splitting the legacy monolithic
 * templates file into workflow-focused modules.
 */
import type { SkillTemplate, CommandTemplate } from '../types.js';

export function getApplyChangeSkillTemplate(): SkillTemplate {
  return {
    name: 'openspec-apply-change',
    description: '实现 OpenSDD 变更中的任务。当用户想要开始实现、继续实现或处理任务时使用。',
    instructions: `实现 OpenSDD 变更中的任务。

**输入**：可选指定变更名称。如果省略，检查是否可以从对话上下文中推断。如果模糊或不明确，你**必须**提示获取可用变更。

**步骤**

1. **选择变更**

   如果提供了名称，使用它。否则：
   - 如果用户提到了某个变更，从对话上下文中推断
   - 如果只存在一个活动变更，自动选择
   - 如果不明确，运行 \`opensdd list --json\` 获取可用变更，并使用 **AskUserQuestion tool** 让用户选择

   始终宣布："正在使用变更：<name>"以及如何覆盖（例如，\`/opsx:apply <other>\`）。

2. **检查状态以了解 Schema**
   \`\`\`bash
   opensdd status --change "<name>" --json
   \`\`\`
   解析 JSON 以了解：
   - \`schemaName\`：正在使用的工作流 Schema（例如："spec-driven"）
   - 哪个产出物包含任务（对于 spec-driven 通常是 "plan"，检查其他产出物的状态）

3. **获取应用指令**

   \`\`\`bash
   opensdd instructions apply --change "<name>" --json
   \`\`\`

   这返回：
   - \`contextFiles\`：产出物 ID -> 具体文件路径数组（因 Schema 而异，可能是 specs/design/plan 或 spec/tests/implementation/docs）
   - 进度（总计、完成、剩余）
   - 带有状态的任务列表
   - 基于当前状态的动态指令

   **处理状态：**
   - 如果 \`state: "blocked"\`（缺少产出物）：显示消息，建议使用 \`openspec-continue-change\`
   - 如果 \`state: "all_done"\`：祝贺，建议归档
   - 否则：继续实现

4. **阅读上下文文件**

   阅读 apply instructions 输出中 \`contextFiles\` 列出的每个文件路径。
   文件取决于正在使用的 Schema：
   - **spec-driven**: specs, design, plan
   - 其他模式：遵循 CLI 输出中的 contextFiles

5. **显示当前进度**

   显示：
   - 正在使用的 Schema
   - 进度："N/M 任务已完成"
   - 剩余任务概览
   - 来自 CLI 的动态指令

6. **实现任务（循环直到完成或受阻）**

   对于每个待处理任务：
   - 显示正在处理哪个任务
   - 进行所需的代码更改
   - 保持更改最小化且专注
   - 在任务文件中标记任务完成：\`- [ ]\` → \`- [x]\`
   - 继续下一个任务

   **暂停如果：**
   - 任务不清楚 → 询问澄清
   - 实现揭示了设计问题 → 建议更新产出物
   - 遇到错误或阻碍 → 报告并等待指导
   - 用户中断

7. **完成或暂停时，显示状态**

   显示：
   - 本次会话完成的任务
   - 总体进度："N/M 任务已完成"
   - 如果全部完成：建议归档
   - 如果暂停：解释原因并等待指导

**实现期间的输出**

\`\`\`
## 正在实现：<change-name>（Schema：<schema-name>）

正在处理任务 3/7：<task description>
[...正在进行实现...]
✓ 任务完成

正在处理任务 4/7：<task description>
[...正在进行实现...]
✓ 任务完成
\`\`\`

**完成时的输出**

\`\`\`
## 实现完成

**变更：** <change-name>
**Schema：** <schema-name>
**进度：** 7/7 任务已完成 ✓

### 本次会话已完成
- [x] 任务 1
- [x] 任务 2
...

所有任务已完成！准备归档此变更。
\`\`\`

**暂停时的输出（遇到问题）**

\`\`\`
## 实现暂停

**变更：** <change-name>
**Schema：** <schema-name>
**进度：** 4/7 任务已完成

### 遇到的问题
<问题描述>

**选项：**
1. <选项 1>
2. <选项 2>
3. 其他方法

您想怎么做？
\`\`\`

**护栏**
- 继续执行任务直到完成或受阻
- 开始前始终阅读上下文文件（来自 apply instructions 输出）
- 如果任务模棱两可，暂停并在实现前询问
- 如果实现揭示了问题，暂停并建议更新产出物
- 保持代码更改最小化并限定在每个任务范围内
- 完成每个任务后立即更新任务复选框
- 遇到错误、阻碍或不清楚的需求时暂停 - 不要猜测
- 使用 CLI 输出中的 contextFiles，不要假设特定的文件名

**流畅的工作流集成**

此技能支持"变更上的操作"模型：

- **可以随时调用**：在所有产出物完成之前（如果存在任务），部分实现之后，与其他操作交错
- **允许产出物更新**：如果实现揭示了设计问题，建议更新产出物 - 不是阶段锁定的，流畅地工作`,
    license: 'MIT',
    compatibility: '需要 opensdd CLI。',
    metadata: { author: 'openspec', version: '1.0' },
  };
}

export function getOpsxApplyCommandTemplate(): CommandTemplate {
  return {
    name: 'OPSX: Apply',
    description: '实现 OpenSDD 变更中的任务（实验性）',
    category: 'Workflow',
    tags: ['workflow', 'artifacts', 'experimental'],
    content: `实现 OpenSDD 变更中的任务。

**输入**：可选择指定变更名称（例如，\`/opsx:apply add-auth\`）。如果省略，检查是否可以从对话上下文中推断出来。如果模糊或不明确，你必须提示可用的变更。

**步骤**

1. **选择变更**

   如果提供了名称，使用它。否则：
   - 如果用户提到了某个变更，从对话上下文中推断
   - 如果只存在一个活动变更，自动选择
   - 如果不明确，运行 \`opensdd list --json\` 获取可用变更，并使用 **AskUserQuestion tool** 让用户选择

   始终宣布："正在使用变更：<name>"以及如何覆盖（例如，\`/opsx:apply <other>\`）。

2. **检查状态以了解 Schema**
   \`\`\`bash
   opensdd status --change "<name>" --json
   \`\`\`
   解析 JSON 以了解：
   - \`schemaName\`：正在使用的工作流 Schema（例如："spec-driven"）
   - 哪个产出物包含任务（对于 spec-driven 通常是 "plan"，检查其他产出物的状态）

3. **获取应用指令**

   \`\`\`bash
   opensdd instructions apply --change "<name>" --json
   \`\`\`

   这返回：
   - \`contextFiles\`：产出物 ID -> 具体文件路径数组（因 Schema 而异）
   - 进度（总计、完成、剩余）
   - 带有状态的任务列表
   - 基于当前状态的动态指令

   **处理状态：**
   - 如果 \`state: "blocked"\`（缺少产出物）：显示消息，建议使用 \`/opsx:continue\`
   - 如果 \`state: "all_done"\`：祝贺，建议归档
   - 否则：继续实现

4. **阅读上下文文件**

   阅读 apply instructions 输出中 \`contextFiles\` 列出的每个文件路径。
   文件取决于正在使用的 Schema：
   - **spec-driven**: specs, design, plan
   - 其他模式：遵循 CLI 输出中的 contextFiles

5. **显示当前进度**

   显示：
   - 正在使用的 Schema
   - 进度："N/M 任务已完成"
   - 剩余任务概览
   - 来自 CLI 的动态指令

6. **实现任务（子智能体驱动，逐任务审查）**

   **核心原则：** 每个任务一个全新子智能体 + 两阶段审查（先规格后质量）= 高质量、快速迭代。

   **无人值守原则：** 一旦进入实现阶段，自主推进所有任务直到全部完成。遇到问题时自行解决，不暂停等待用户介入。

   对于每个待处理任务，按以下子步骤执行：

   **6.1 准备任务上下文**

   从计划文件中提取当前任务的完整文本（含所有步骤和代码片段）。
   不让子智能体读取文件——你提供完整文本。
   跳过场景铺设上下文是红线——子智能体需要理解任务在哪个环节。
   记录此任务的场景铺设上下文：
   - 此任务在整个变更中的位置
   - 前置任务已完成的内容
   - 相关的架构决策（来自 design.md）
   - 相关的需求规格（来自 spec.md，如有）

   **6.2 分派实现子智能体**

   使用 Task tool (code-generator) 分派实现子智能体：
   - subagent_type: "code-generator"
   - description: "实现任务 N/M：<任务摘要>"
   - prompt 包含：完整任务文本 + 场景铺设上下文 + 工作目录
   - code-generator agent 已包含专业的代码生成 system prompt，你只需在 prompt 中补充以下任务协调指令：
     - 汇报格式：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
     - 自审要求：完整性、质量、纪律、测试检查
     - 步骤执行纪律：严格按计划步骤顺序，不合并、跳过或重排
     - 提问机制：开始前可提问澄清需求
     - 上报机制：力不从心时停下来上报

   等待实现子智能体完成。

   **如果子智能体提问：**
   - 清晰完整地回答
   - 必要时提供额外上下文
   - 不要催促它们进入实现阶段

   **6.3 处理实现者状态**

   实现者返回四种状态之一：
   - **DONE：** 进入规格合规性审查。
   - **DONE_WITH_CONCERNS：** 实现者完成了工作但标记了疑虑。在继续之前阅读这些疑虑。如果疑虑涉及正确性或范围，在审查前解决。如果只是观察性说明（如"这个文件越来越大了"），记录下来并继续审查。
   - **NEEDS_CONTEXT：** 实现者需要未提供的信息。提供缺失的上下文并重新分派。
   - **BLOCKED：** 实现者无法完成任务。自主评估并解决，不暂停等待用户：
     1. 如果是上下文问题，提供更多上下文并用同一模型重新分派
     2. 如果任务需要更强的推理能力，用更强的模型重新分派
     3. 如果任务太大，拆分为更小的部分
     4. 如果计划本身有问题，根据已有上下文自行做出合理判断继续推进

   **绝不**忽略上报或在不做任何更改的情况下让同一模型重试。如果实现者说卡住了，说明有什么东西需要改变。

   **如果子智能体失败：**
   - 分派修复子智能体并提供具体指令
   - 不要尝试手动修复（上下文污染）

   **6.4 分派规格合规审查子智能体**

   **仅在实现者状态为 DONE 或 DONE_WITH_CONCERNS（疑虑已处理）后才执行此步。**

   使用 Task tool 分派规格审查子智能体，参考 \`subagent-driven-development/spec-reviewer-prompt.md\` 模板：
   - description: "审查任务 N/M 的规格合规性"
   - prompt 包含：任务需求完整文本 + 实现者报告 + SPEC.md 路径（如有）

   规格审查者独立验证实现代码，不信任实现者报告。检查：缺失的需求、多余/不需要的工作、理解偏差。通过阅读代码来验证，而非信任报告。

   如果规格审查者发现 ❌ 问题：
   - 让实现子智能体修复问题
   - 重新分派规格审查子智能体
   - 重复直到 ✅ 符合规格
   - 不要跳过重新审查

   **绝不**带着未通过的规格审查进入下一步。
   **绝不**在规格合规性上接受"差不多就行"（规格审查者发现问题 = 未完成）。

   **6.5 分派代码质量审查子智能体**

   **仅在规格合规审查通过后才执行此步。**（顺序错误 = 红线）

   使用 Task tool 分派代码质量审查子智能体，参考 \`subagent-driven-development/code-quality-reviewer-prompt.md\` 模板：
   - description: "验证任务 N/M 的编译和测试"
   - prompt 包含：任务摘要 + Git SHA 范围

   代码质量审查者仅执行两项检查：编译检查（构建命令无编译错误）和测试检查（单元测试全部通过）。

   如果代码审查者发现 ❌ 问题：
   - 让实现子智能体修复
   - 重新分派代码质量审查
   - 重复直到 ✅ 通过
   - 不要跳过重新审查

   **绝不**带着编译失败或测试失败的代码标记任务完成。

   **6.6 标记任务完成并继续**

   两阶段审查均通过后（自审 + 正式审查，两者都需要）：
   - 在任务文件中标记：\`- [ ]\` → \`- [x]\`
   - 显示："任务 N/M 完成 ✓（规格 ✅ 代码质量 ✅）"
   - 自动继续下一个待处理任务（回到 6.1），不等待用户确认

7. **完成时，显示状态（带变更级验证）**

   所有任务完成后，执行验证门控。铁律：**没有新鲜的验证证据，不许宣称完成。**

   如果你在验证时没有运行命令，就不能声称所有任务已完成。跳过任何一步 = 说谎，不是验证。

   验证门控：
   1. **确定：** 什么命令能证明所有变更已正确完成？
   2. **运行：** 执行项目的完整构建命令和完整测试套件（重新运行，完整执行）
   3. **阅读：** 完整输出，检查退出码，统计失败数
   4. **验证：** 输出是否支持"变更已完成"这个结论？
      - 如果否：用证据说明实际状态，自行修复问题，重新验证
      - 如果是：带证据陈述结论
   5. **只有这时：** 才能宣称完成

   常见失败模式——以下证据**不够格**：
   - "之前的运行结果"、"应该会通过" → 不够格，需要新鲜的测试命令输出
   - 部分检查、推断 → 不够格，需要完整输出
   - 代理报告"成功" → 不够格，需要 VCS diff 或构建输出
   - "测试通过了" → 不够格，需要逐项核对清单

   红线——停下来重新验证：
   - 使用"应该"、"大概"、"似乎"
   - 验证前就表达满意（"太好了！"、"完美！"、"搞定！"等）
   - **任何暗示成功但实际未运行验证的措辞**

   防止合理化：
   - "应该能行了" → 运行验证命令
   - "我有信心" → 信心 ≠ 证据
   - "代理说成功了" → 独立验证
   - "部分检查就够了" → 部分检查什么也证明不了

   **验证没有捷径。运行命令。阅读输出。然后才能宣称结果。这没有商量余地。**

   验证通过后显示：
   - 本次会话完成的任务（附审查结果）
   - 总体进度："N/M 任务已完成"
   - 变更级验证结果：构建 ✅/❌（附命令和 exit code），测试 ✅/❌（附通过数）
   - 建议归档

   **验证失败时：**
   - 自行修复问题，不暂停等待用户
   - 修复后重新执行验证门控
   - 只有在反复修复仍无法通过时才暂停报告

**实现期间的输出**

\`\`\`
## 正在实现：<change-name>（Schema：<schema-name>）

正在处理任务 3/7：<task description>
→ 分派实现子智能体...
→ 实现者完成（状态：DONE）
→ 分派规格合规审查...
→ 规格审查：✅ 符合规格
→ 分派代码质量审查...
→ 代码质量审查：✅ 编译通过，测试通过
✓ 任务 3/7 完成（规格 ✅ 代码质量 ✅）

正在处理任务 4/7：<task description>
→ 分派实现子智能体...
→ 实现者完成（状态：DONE_WITH_CONCERNS："文件较大"）
→ 疑虑评估：观察性说明，记录并继续
→ 分派规格合规审查...
→ 规格审查：❌ 缺失进度报告
→ 实现者修复中...
→ 规格审查（重新）：✅ 符合规格
→ 分派代码质量审查...
→ 代码质量审查：✅ 通过
✓ 任务 4/7 完成（规格 ✅ 代码质量 ✅，一轮规格修复）
\`\`\`

**完成时的输出**

\`\`\`
## 实现完成

**变更：** <change-name>
**Schema：** <schema-name>
**进度：** 7/7 任务已完成 ✓

### 变更级验证
- **构建：** \`npm run build\` → exit 0 ✅
- **测试：** \`npm test\` → 34/34 pass ✅

### 本次会话已完成
- [x] 任务 3：<description>（规格 ✅ 代码质量 ✅）
- [x] 任务 4：<description>（规格 ✅ 代码质量 ✅，一轮规格修复）
...

所有任务已完成并通过验证！可以使用 \`/opsx:archive\` 归档此变更。
\`\`\`

**暂停时的输出（仅在反复自主修复仍无法通过时）**

\`\`\`
## 实现暂停——需要人工介入

**变更：** <change-name>
**Schema：** <schema-name>
**进度：** 4/7 任务已完成

### 已自主尝试的解决措施
1. <尝试 1：例如，换更强模型重新分派>
2. <尝试 2：例如，拆分任务后重新分派>
3. <尝试 3：例如，提供额外上下文重新分派>

### 仍无法解决的问题
任务 5：<具体阻塞原因>

**已完成任务：**
- [x] 任务 1-4

**选项：**
1. <基于问题性质的具体建议>
2. 其他方法
\`\`\`

**护栏**

**无人值守纪律：**
- 一旦进入实现阶段，自主推进所有任务直到全部完成
- 遇到 BLOCKED 时自行解决：提供上下文、换更强模型、拆分任务、自行判断
- 遇到验证失败时自行修复，修复后重新验证
- 不在任务中途暂停等待用户确认或指导
- 只有在反复尝试仍无法通过时才暂停报告状态

**上下文纪律：**
- 开始前始终阅读上下文文件（来自 apply instructions 输出）
- 使用 CLI 输出中的 contextFiles，不要假设特定的文件名
- 不让子智能体读取计划文件——你提供完整文本
- 保持代码更改最小化并限定在每个任务范围内

**子智能体纪律：**
- 每个任务一个全新子智能体，隔离上下文，不继承会话历史
- 不并行分派多个实现子智能体（会冲突）
- 按顺序执行：实现 → 规格审查 → 代码质量审查
- 不跳过任何审查阶段
- 不让实现者的自审替代正式审查（两者都需要）
- 在规格合规审查通过之前不开始代码质量审查（顺序错误 = 红线）
- 如果审查者发现问题：实现者修复 → 重新审查 → 重复直到通过，不跳过重新审查
- 不在任一审查有未解决问题时进入下一个任务
- 实现者子智能体严格按计划中的步骤顺序执行，不合并、不跳过、不重排

**验证纪律（铁律）：**
- **没有新鲜的验证证据，不许宣称完成**
- 所有任务完成后，亲自运行构建和测试，阅读输出，确认通过后才宣称完成
- 不信任子智能体的成功报告——代理报告成功 → 检查 VCS diff → 验证变更 → 报告实际状态
- 不使用"应该"、"大概"、"似乎"、"应该能行了"等措辞
- 在看到实际验证输出之前不表达满意
- 跳过验证任何一步 = 说谎，不是验证
- 信心 ≠ 证据，部分检查什么也证明不了，没有例外

**流畅的工作流集成**

此技能支持"变更上的操作"模型：

- **可以随时调用**：在所有产出物完成之前（如果存在任务），部分实现之后，与其他操作交错
- **允许产出物更新**：如果实现揭示了设计问题，建议更新产出物 - 不是阶段锁定的，流畅地工作`
  };
}
